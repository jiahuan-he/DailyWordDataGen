For the word "{word}" ({selected_pos}), you are given 4 generated example sentences and their Chinese translations.

Your task is to:
1. Extract the exact Chinese translation substring.
2. Evaluate all 4 sentences against a unified quality rubric and rank them (1 to 4).

Examples to evaluate:
{examples_json}

*** RULES FOR EXTRACTION (translated_word) ***
1. The `translated_word` MUST be an EXACT substring of the provided Chinese translation. Do not invent or modify words.
2. Choose the most specific and concise translation (do not highlight the whole phrase if a single word or 2-4 character term suffices).
3. Ensure the `translated_word` is a complete and natural Chinese semantic unit (完整词汇). For example, extract "一代人" or "代", but avoid awkward partial boundaries like "代人".
4. Only output `null` as an absolute last resort — when the English word's meaning is completely dissolved into the Chinese phrasing with no identifiable substring. In most cases, a 1-4 character extraction is possible.
5. If the translation uses a synonym or rephrasing, extract the most semantically equivalent substring.

*** RULES FOR RANKING (display_order) ***
Rank the 4 sentences using a "clear → deep" progression. The order is NOT "best → worst quality" but a deliberate learning journey:

- `display_order: 1` (Hero Card) — The sentence a first-time learner sees. Must clearly demonstrate the word's core meaning AND be beautiful. A learner who has never seen this word should understand it from this sentence alone. Prefer natural, grounded sentences over abstract/metaphorical ones. Avoid compound idioms where the target word is buried (e.g., "jam session" for "session").
- `display_order: 2` — A sentence that adds emotional depth or a memorable perspective. Can be more literary.
- `display_order: 3` — A sentence that explores a nuanced or metaphorical usage. Can be abstract/philosophical.
- `display_order: 4` — The most creative or unconventional usage. Fine to be challenging.

All 4 sentences should still meet basic quality standards: brevity, elegant Chinese translation, and natural usage of the word.

Output as JSON:
{{
  "results": [
    {{
      "original_index": 0,
      "translated_word": "...",
      "display_order": 2
    }},
    {{
      "original_index": 1,
      "translated_word": null,
      "display_order": 1
    }}
  ]
}}